export interface CabinetStatisticsObjects {
	objects_registered: number,
	objects_published: number,
	objects_rejected: number,
	objects_blocked: number
}

export interface CabinetStatisticsUsers {
	users_blocked: number,
	users_registered: number
}